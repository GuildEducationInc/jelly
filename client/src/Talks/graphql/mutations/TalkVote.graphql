mutation TalkVote($id: ID!, $direction: Direction!) {
  talkVote(input: { id: $id, direction: $direction }) {
    talk {
      id
      gid
      topic
      votes
      description
    }
    errors
  }
}
